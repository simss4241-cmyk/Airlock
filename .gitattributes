# Normalise to LF in the repo; check out native.
* text=auto

# Windows launchers must be CRLF on disk or they misbehave when run.
*.bat  text eol=crlf
*.ps1  text eol=crlf
*.vbs  text eol=crlf

# Binary
*.png  binary
*.ico  binary
*.db   binary
